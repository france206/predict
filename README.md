# predict
app based on python to analyzed soccer matches 
To open predict save it on your pc in a directory with alla the others files. Saved all the files "name.py". Then open your Cmd:

Write " python name.py" to start a file

To start predict.py wite on your Cmd "streamlit run predict.py"
